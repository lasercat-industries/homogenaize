{
  "name": "homogenaize",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "tsc --noEmit",
    "format": "echo 'Format script not configured yet'"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "vite": "^7.0.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "zod": "^4.0.0"
  }
}
